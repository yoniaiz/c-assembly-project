#include "../header.h"

void first_loop(
    commands *cmd,
    data_row **data,
    memory_row **memory,
    symbol_row **symbol_table);

void second_loop(data_row *data, memory_row *memory, symbol_row *symbol_table);